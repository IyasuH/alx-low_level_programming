#include "main.h"
#include <stdio.h>
/**
 *_strcpy - func
 *@dest: char
 *@src: char 1
 *
 *Return : null
 */
char *_strcpy(char *dest, char *src)
{
  int i;
  for(i = 0; src[i] != '\0'; i++)
    dest[i] = src[i];
  else
    dest[i] = '\0';
  return (dest);
}
